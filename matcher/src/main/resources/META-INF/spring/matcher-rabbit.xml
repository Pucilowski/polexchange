<?xml version="1.0" encoding="UTF-8"?>
<!--
    Application context definition for PetClinic Datasource.
-->
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/rabbit
         http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

    <!-- ========================= DATASOURCE DEFINITION ========================= -->

    <bean id="jsonMessageConverter" class="org.springframework.amqp.support.converter.JsonMessageConverter"/>
    <rabbit:connection-factory id="connectionFactory"/>
    <rabbit:template id="matcherRabbit" connection-factory="connectionFactory"
                     message-converter="jsonMessageConverter"
                     exchange="orders" routing-key="foo.bar"/>

    <!--    <rabbit:admin connection-factory="connectionFactory" />

        <rabbit:queue name="myQueue" />

        <rabbit:topic-exchange name="myExchange">
            <rabbit:bindings>
                <rabbit:binding queue="myQueue" pattern="orders.submitted.*" />
            </rabbit:bindings>
        </rabbit:topic-exchange>-->

    <!--<bean id="orderListener" class="com.pucilowski.exchange.matcher.integration.service.OrderListenerImpl"/>-->
    <rabbit:listener-container connection-factory="connectionFactory" message-converter="jsonMessageConverter">
        <rabbit:listener ref="matchBroadcasterImpl" method="orderSubmitted" queue-names="order.submitted.ltc.btc"/>
        <rabbit:listener ref="matchBroadcasterImpl" method="orderCancelled" queue-names="order.cancelled.ltc.btc"/>
    </rabbit:listener-container>


</beans>