<?xml version="1.0" encoding="UTF-8"?>
<!--
    Application context definition for PetClinic Datasource.
-->
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation=
               "http://www.springframework.org/schema/beans
                http://www.springframework.org/schema/beans/spring-beans.xsd
                http://www.springframework.org/schema/rabbit
                http://www.springframework.org/schema/rabbit/spring-rabbit.xsd">

    <!-- ========================= DATASOURCE DEFINITION ========================= -->


    <rabbit:connection-factory id="connectionFactory"
                               host="localhost" username="guest" password="guest"/>

    <rabbit:admin id="rabbitAdmin" connection-factory="connectionFactory"/>

    <rabbit:template id="rabbitTemplate" connection-factory="connectionFactory"
                     message-converter="json" exchange="orders"/>


    <!--    <rabbit:direct-exchange name="orders"/>
        <rabbit:queue name="orders.submit"/>

        <rabbit:topic-exchange name="orders">
            <rabbit:bindings>
                <rabbit:binding queue="orders.submit" pattern="orders.submit.#"/>
            </rabbit:bindings>
        </rabbit:topic-exchange>-->


    <rabbit:listener-container connection-factory="connectionFactory" message-converter="json">
        <rabbit:listener ref="messageQueue" method="onOrderSubmitted" queue-names="orders.trades"/>
        <!--<rabbit:listener ref="messageQueue" method="onOrderSubmitted" queue-names="orders.trades" />-->
    </rabbit:listener-container>

    <bean id="json" class="org.springframework.amqp.support.converter.JsonMessageConverter"/>

    <bean id="messageQueue" class="com.pucilowski.exchange.main.service.matcher.MatcherListener"/>

</beans>